import type { Component, QueryResult } from '../lib/types';

/** Stub — Task 14 fills this in with the real chart. */
export function TimeSeriesChart(_props: { component: Component; data: QueryResult | null }) {
  return null;
}
